spring:
  # DB接続先設定
  datasource:
  # ecsでpostgresコンテナ利用の場合
    url : jdbc:postgresql://localhost:5432/schedule_board
    username : postgres
    password : postgres
    driver-class-name : org.postgresql.Driver
  jpa:
    database-platform : org.hibernate.dialect.PostgreSQLDialect
    show-sql : false
    hibernate.ddl-auto : none

    